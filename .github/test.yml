name: Test

on: [push, pull_request]

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}

      - name: Request Google Test
        uses: ./
        with:
          url: 'https://google.de'
          method: 'GET'
